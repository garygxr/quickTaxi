server:
  port: 8001


spring:
  application:
    name: quickTaxi-verificationCode
  cloud:
    # nacos
    nacos:
      # 服务发现
      discovery:
        server-addr: localhost:8848
        group: quickTaxi


      # 配置中心
      config:
        server-addr: localhost:8848
        group: quickTaxi

